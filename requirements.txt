# Core pipeline. Sufficient to run the detector and produce JSON.
ultralytics>=8.3.0        # YOLOv8 runtime for the OmniParser icon detector
opencv-python>=4.9.0
numpy>=1.26.0
pillow>=10.0.0
huggingface-hub>=0.24.0   # weight download
